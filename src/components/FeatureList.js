import React from 'react';
import { View, Text } from 'react-native';

const FeatureList = () => {
    return (
        <View>
            <Text>Reader</Text>
        </View>
    );
};

export default FeatureList;
